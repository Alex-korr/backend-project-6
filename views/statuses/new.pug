//- New status page
extends ../layout.pug

block content
  h1 Create status
  form(action='/statuses', method='POST')
    .mb-3
      label.form-label(for='name') Name
      input.form-control(type='text', name='name', id='name', required, minlength='1')
    // Error alert removed
    button.btn.btn-primary(type='submit') Create
